// User presence tracking - disabled until user_presence table is created
export const useUserPresence = () => {
  // This hook is disabled because the user_presence table doesn't exist yet
  // When you create the table, implement presence tracking here
};
