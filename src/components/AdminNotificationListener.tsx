// Admin notification listener - disabled until admin_notifications table is created
export const AdminNotificationListener = () => {
  // This component is disabled because the required database tables don't exist yet
  // Required tables: admin_notifications, notification_reads
  return null;
};
